[test]
# Single-process runner — all tests share state
# Clean up after yourself: process.env, console, mocks
timeout = 30000
root = "."

# Exclude faf-engine (legacy, missing deps in CI)
exclude = ["faf-engine", "node_modules"]
